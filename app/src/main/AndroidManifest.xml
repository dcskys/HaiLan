<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dc.hailan">

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".modules.assist.GuideViewPager" />
        <activity android:name=".utils.greendao.demo.NoteActivity" />
        <activity android:name=".DemoActivity" />
        <activity android:name=".modules.SplashActivity" />
        <activity android:name=".modules.WelcomeActivity" />

        <activity android:name=".modules.BottomMenuActivity" />

        <!--<activity android:name=".modules.CloudMainMenuActivity"></activity>-->


    </application>

</manifest>